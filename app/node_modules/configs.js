const path = require('path');

const rootDir = path.resolve(__dirname, '../../');
const env = process.env.NODE_ENV || 'default';

const configs = {
    production: {
        auth: {
            facebook: {
                successRedirect: '/account',
                failureRedirect: '/unauthorized',
            },
        },
    },
    default: {
        cookieSecret: 'your cookie secret goes here',
        auth: {
            facebook: {
                appId: 'YOUR_APPID',
                appSecret: 'YOUR_APPSECRET',
                // successRedirect: '/account',
                redirects: {
                    failureRedirect: '/login',
                    successReturnToOrRedirect: '/account',
                },
                callbackURL: '/auth/facebook/callback',
            },
        },
        viewsDir: path.join(rootDir, './app/views'),
        layoutsDir: path.join(rootDir, './app/views/layouts'),
        partialsDir: path.join(rootDir, './app/views/partials'),
        staticDir: path.join(rootDir, './public'),
        uploadsDir: path.join(rootDir, './uploads'),
        favIconPath: path.join(rootDir, 'public', 'img', 'favicon.ico'),
        cssScssDirConfigs: {
            publicBuildCssDir: '../public/build/css',
            publicBuildCssMapsDir: '../sourcemaps', // path is relative to publicBuildCssDir
            publicSassDir: './styles/**/*.scss',
        },
    },
};

module.exports = configs[env];
